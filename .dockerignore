node_modules
.next
.git
.vscode
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*.local
coverage
*.md

# .env IS intentionally included in the build context: NEXT_PUBLIC_* values
# must be present at `next build` time to be inlined into the client bundle.
# It is never copied into the final runner stage of the Dockerfile.
